{% extends 'MunicipalesBundle:theme:layout.html.twig' %}

{% block title %}{{ 'candidate.html_title'|trans }}{% endblock %}
{% block content %}
    <h1>{{ 'candidate.h1_title'|trans }}</h1>
    
    <p class="important">
    	<div>
			{{ form_start(form, { 'attr': {'class': 'form_settings'}}) }}
			
			<p>{{ form_errors(form) }}<p>
			
			{% if errors is defined and not errors is empty %}
				<div style="background-color:gold;color:black">{{ errors }}</div>
			{% endif %}

			<p>
				Bienvenido o bienvenida a el alta de candidatos para la candidatura ciudadana <b>{{ address|capitalize }}</b> al municipio de <b>{{ town|capitalize }}</b>. 
				Por favor rellena tu candidatura con exactitud, si te equivocas o quieres modificarla más adelante tendrás que rellenar de nuevo el cuestionario.
			</p>
			
			<p>
				El plazo de presentación de candidaturas establecido es desde {{ to }} hasta {{ from }}
			</p>
			
			<p>
				<span>{{ form_label(form.name, 'candidacy.step1.name'|trans) }}</span>
				<span>{{ form_errors(form.name) }}</span>
				{{ form_widget(form.name) }}
			</p>
			
			<p>
			    <span>{{ form_label(form.lastname, 'candidacy.step1.lastname'|trans) }}</span>
			    <span>{{ form_errors(form.lastname) }}</span>
			    {{ form_widget(form.lastname) }}
			</p>
			
			<p>
			    <span>{{ form_label(form.dni, 'candidacy.step1.dni'|trans) }}</span>
			    <span>{{ form_errors(form.dni) }}</span>
			    {{ form_widget(form.dni) }}
			</p>
			
			<p>
			    <span>{{ form_label(form.email, 'candidacy.step1.email'|trans) }}</span>
			    <span>{{ form_errors(form.email) }}</span>
			    {{ form_widget(form.email) }}
			</p>

			<p>
			    <span>{{ form_label(form.phone, 'candidacy.step1.phone'|trans) }}</span>
			    <span>{{ form_errors(form.phone) }}</span>
			    {{ form_widget(form.phone) }}
			</p>
			
			<p>
			    {{ form_widget(form.continue, { 'label': 'candidacy.step1.continue'|trans }) }}
			</p>
			
			{{ form_rest(form) }}
			
			{{ form_end(form) }}
		</div>
    </p>
{% endblock %}